# Origin of the Lever API. The dashboard is deployed separately, on its own
# domain, so this is always a cross-origin call — the API must name this
# dashboard's origin in LEVER_ADMIN_ORIGINS and LEVER_WEBAUTHN_ORIGINS.
VITE_API_BASE_URL=http://localhost:3000
